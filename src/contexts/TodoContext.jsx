import { createContext } from "react";

console.log("***** createContext Renderd ****");
const TodoContext = createContext([]);

export default TodoContext;

// title: "",
// handleChange: null,
// currentLoanForm: null,
